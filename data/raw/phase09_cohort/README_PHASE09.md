# Phase 09 raw-data staging directory

Raw third-party photometry is deliberately excluded from the redistributable release.
Run `experiments/fetch_phase09_sources.py` with explicit attribution acknowledgement
in a network-enabled environment. The retriever verifies the frozen Git commit, path,
Git blob SHA-1, expected byte count, and records a local SHA-256 acquisition receipt.

Phase 09 must not compute its family-level primary outputs until all 15 declared
exposed-development identities have claim-grade period/mode metadata and verified
source bytes. Partial/cached results are retained only as inherited development evidence.
