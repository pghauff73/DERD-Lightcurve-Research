# DERD v1.4 release notes

## Phase 14 period-coordinate robustness ledger

Release identity:

```text
DERD-v1.4-phase14-period-coordinate-robustness-ledger
```

Decision:

```text
PHASE14_PERIOD_ROBUSTNESS_LEDGER_UPDATED_POPULATION_GATE_CLOSED
```

Phase 14 adds `OGLE-LMC-CEP-0002` as the fourth cumulative exposed-development record. The target was selected from the frozen Phase-08 acquisition ranking after excluding the three Phase-13 ledger identities. Its target-level scientific projection and signed harmonic exchange reproduce the Phase-08 record exactly.

The new coordinate-robustness tier refines the period with an independent phase-dispersion objective and then repeats the chronological h1-h4 stationarity audit at the catalog and refined periods. The period changes from `3.1181490000` to `3.1180866370` days. Phase dispersion improves from `0.091786` to `0.083504`, while the temporal score falls only from `2.385644` to `2.328325`. Both remain above their separately calibrated stationary thresholds.

The resulting classification is:

```text
TEMPORAL_STATIONARITY_FAILURE_ROBUST_TO_PERIOD_REFINEMENT
```

No chronological block measures all four recovery harmonics above SNR 3. The base DERD score `2.572199` also remains above its target-specific compatibility threshold `2.094903`.

The cumulative ledger now contains 4 of 15 objects: two classical Cepheids and two RR Lyrae stars. C17, family fractions, population inference, internal-mechanism claims, the universal transparent-shell claim, and shell-mass claims remain closed.
