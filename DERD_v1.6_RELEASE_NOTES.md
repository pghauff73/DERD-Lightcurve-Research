# DERD v1.6 release notes

## Phase 16 cross-version reproducibility graph

Release identity:

```text
DERD-v1.6-phase16-reproducibility-graph
```

Decision:

```text
PHASE16_REPRODUCIBILITY_GRAPH_SEALED_POPULATION_GATE_CLOSED
```

Phase 16 constructs a reproducibility graph across the five-object cumulative ledger. Nine software-analysis nodes collapse to five astronomical denominator records. Three same-source, same-configuration comparisons reproduce exactly apart from metadata transport labels. One same-source comparison shows configuration-sensitive scientific drift. One object has only a single retained version. No edge qualifies as external independent astrophysical replication.

The multiplicity guard prevents four repeated analyses from inflating the astronomical denominator. Family fractions, population comparisons, internal-mechanism claims, transparent-shell claims, and shell-mass claims remain closed.
