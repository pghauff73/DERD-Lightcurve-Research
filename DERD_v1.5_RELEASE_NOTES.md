# DERD v1.5 release notes

## Phase 15 archival lineage promotion

Release identity:

```text
DERD-v1.5-phase15-archival-lineage-promotion
```

Decision:

```text
PHASE15_ARCHIVAL_LINEAGE_PROMOTED_LEDGER_UPDATED_POPULATION_GATE_CLOSED
```

Phase 15 promotes `OGLE-LMC-CEP-0010` from the already source-verified Phase-07/08 archive into the cumulative evidence ledger. The source repository, commit, path, Git blob, source SHA-256, byte count, observation count, target-record hash, and harmonic-exchange hash all pass.

The same source does not produce an exact scientific replay across Phase 07 and Phase 08. The maximum harmonic-SNR difference is approximately `49.5471`, the screen score changes by `0.0233973`, the stage changes from `FORECAST_HARMONICS` to `RECOVERY_HARMONICS`, and the disposition changes. The comparison is therefore retained as configuration-sensitive scientific drift, not counted as an independent astrophysical replication.

The cumulative ledger now contains five unique objects: three classical Cepheids and two RR Lyrae stars. Population outputs remain suppressed.
